import { NextResponse } from 'next/server'; import { db,sessionUser } from '@/lib/server';
export async function GET(){const me:any=await sessionUser();if(!me)return NextResponse.json({error:'Unauthorized'},{status:401});const {data,error}=await db.from('notifications').select('*').eq('company_id',me.company_id).eq('user_id',me.id).order('created_at',{ascending:false}).limit(50);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({notifications:(data||[]).map((n:any)=>({id:n.id,type:n.type,title:n.title,message:n.message,esoReportId:n.eso_report_id,isRead:n.is_read,createdAt:n.created_at}))});}
export async function PATCH(req:Request){const me:any=await sessionUser();if(!me)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();let q:any=db.from('notifications').update({is_read:true}).eq('company_id',me.company_id).eq('user_id',me.id);if(b.id)q=q.eq('id',b.id);else q=q.eq('is_read',false);const {error}=await q;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
