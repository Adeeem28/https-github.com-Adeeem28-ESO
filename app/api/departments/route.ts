import { NextResponse } from 'next/server'; import { db, sessionUser, canAdmin } from '@/lib/server';
function cleanCode(value:any){const code=String(value||'').trim().toUpperCase().replace(/[^A-Z0-9_-]/g,'');return code||null;}
export async function POST(req:Request){const me:any=await sessionUser();if(!me||!canAdmin(me.role))return NextResponse.json({error:'Forbidden'},{status:403});const {name,code}=await req.json();const cleanName=String(name||'').trim();if(!cleanName)return NextResponse.json({error:'Department name is required'},{status:400});const {error}=await db.from('departments').insert({company_id:me.company_id,name:cleanName,code:cleanCode(code),active:true});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
export async function PATCH(req:Request){const me:any=await sessionUser();if(!me||!canAdmin(me.role))return NextResponse.json({error:'Forbidden'},{status:403});const b=await req.json();const update:any={};if(b.name!==undefined){const n=String(b.name||'').trim();if(!n)return NextResponse.json({error:'Department name is required'},{status:400});update.name=n;}if(b.code!==undefined)update.code=cleanCode(b.code);if(b.active!==undefined)update.active=!!b.active;const {error}=await db.from('departments').update(update).eq('company_id',me.company_id).eq('id',b.id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
